import { AppContainer } from "@/components/layout/AppContainer"

/**
 * Home - 应用主页面
 *
 * 职责：
 * - 作为应用的入口点
 * - 渲染主要的应用容器
 * - 保持页面组件的简洁性
 */
export default function Home() {
  return <AppContainer />
}
